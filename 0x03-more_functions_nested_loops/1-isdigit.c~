#include "holberton.h"
/**
 *_isdigit - checks for a digit
 *@c: integer to test
 * Return: 1 if digit, otherwise 0
 */
int _isdigit(int c)
{
  if ((c >= 0 && c <= 9) || c == '0')
{
return (1);
}
else
{
return (0);
}
}
