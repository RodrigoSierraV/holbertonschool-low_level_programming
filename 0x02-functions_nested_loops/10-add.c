#include "holberton.h"
/**
 * add - adds two integers
 * @a: first integer to add
 * @b: second integer to add
 * Return: result of the addition
 */
int add(int a, int b)
{
int add = a + b;
return (add);
}
