#include <stdio.h>
#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int get_endianness(void)
{
  int endianness = 1;

  if (*(char *)&endianness == 1)
    return (1);
  else
    return (0);
}
